This file is automatically generated by tools/zap_converter.py. Don't edit it.

Replace `some_endpoint` with a Matter endpoint id or the id assigned to an endpoint. The equivalent explicit `endpoint`, `cluster`, and `command` form is also supported.

# Identify

Attributes and commands for putting a device into Identification mode (e.g. flashing a light).

```yaml
# This command starts or stops the receiving device identifying itself.
matter.send_command:
  path: some_endpoint.identify.identify
  arguments:
    identify_time:

# This command allows the support of feedback to the user, such as a certain light effect.
matter.send_command:
  path: some_endpoint.identify.trigger_effect
  arguments:
    effect_identifier: # enum: blink, breathe, okay, channel_change, finish_effect, stop_effect
    effect_variant: # enum: default
```

# Groups

Attributes and commands for group configuration and manipulation.

```yaml
# The AddGroup command allows a client to add group membership in a particular group for the server endpoint.
matter.send_command:
  path: some_endpoint.groups.add_group
  arguments:
    group_id:
    group_name:

# The ViewGroup command allows a client to request that the server responds with a ViewGroupResponse command containing the name string for a particular group.
matter.send_command:
  path: some_endpoint.groups.view_group
  arguments:
    group_id:

# The GetGroupMembership command allows a client to inquire about the group membership of the server endpoint, in a number of ways.
matter.send_command:
  path: some_endpoint.groups.get_group_membership
  arguments:
    group_list:

# The RemoveGroup command allows a client to request that the server removes the membership for the server endpoint, if any, in a particular group.
matter.send_command:
  path: some_endpoint.groups.remove_group
  arguments:
    group_id:

# The RemoveAllGroups command allows a client to direct the server to remove all group associations for the server endpoint.
matter.send_command: some_endpoint.groups.remove_all_groups

# The AddGroupIfIdentifying command allows a client to add group membership in a particular group for the server endpoint, on condition that the endpoint is identifying itself.
matter.send_command:
  path: some_endpoint.groups.add_group_if_identifying
  arguments:
    group_id:
    group_name:
```

# OnOff

Attributes and commands for switching devices between 'On' and 'Off' states.

```yaml
# On receipt of this command, a device SHALL enter its ‘Off’ state. This state is device dependent, but it is recommended that it is used for power off or similar functions. On receipt of the Off command, the OnTime attribute SHALL be set to 0.
matter.send_command: some_endpoint.on_off.off

# On receipt of this command, a device SHALL enter its ‘On’ state. This state is device dependent, but it is recommended that it is used for power on or similar functions. On receipt of the On command, if the value of the OnTime attribute is equal to 0, the device SHALL set the OffWaitTime attribute to 0.
matter.send_command: some_endpoint.on_off.on

# On receipt of this command, if a device is in its ‘Off’ state it SHALL enter its ‘On’ state. Otherwise, if it is in its ‘On’ state it SHALL enter its ‘Off’ state. On receipt of the Toggle command, if the value of the OnOff attribute is equal to FALSE and if the value of the OnTime attribute is equal to 0, the device SHALL set the OffWaitTime attribute to 0. If the value of the OnOff attribute is equal to TRUE, the OnTime attribute SHALL be set to 0.
matter.send_command: some_endpoint.on_off.toggle

# The OffWithEffect command allows devices to be turned off using enhanced ways of fading.
matter.send_command:
  path: some_endpoint.on_off.off_with_effect
  arguments:
    effect_identifier: # enum: delayed_all_off, dying_light
    effect_variant:

# This command allows the recall of the settings when the device was turned off.
matter.send_command: some_endpoint.on_off.on_with_recall_global_scene

# This command allows devices to be turned on for a specific duration with a guarded off duration so that SHOULD the device be subsequently turned off, further OnWithTimedOff commands, received during this time, are prevented from turning the devices back on.
matter.send_command:
  path: some_endpoint.on_off.on_with_timed_off
  arguments:
    # on_off_control: # bitmap: accept_only_when_on
    on_time:
    off_wait_time:
```

# LevelControl

Attributes and commands for controlling devices that can be set to a level between fully 'On' and fully 'Off.'

```yaml
# This command will move the device to the specified level.
matter.send_command:
  path: some_endpoint.level_control.move_to_level
  arguments:
    level:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# This command will move the device using the specified values.
matter.send_command:
  path: some_endpoint.level_control.move
  arguments:
    move_mode: # enum: up, down
    rate:
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# This command will do a relative step change of the device using the specified values.
matter.send_command:
  path: some_endpoint.level_control.step
  arguments:
    step_mode: # enum: up, down
    step_size:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# This command will stop the actions of various other commands that are still in progress.
matter.send_command:
  path: some_endpoint.level_control.stop
  arguments:
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# Command description for MoveToLevelWithOnOff
matter.send_command:
  path: some_endpoint.level_control.move_to_level_with_on_off
  arguments:
    level:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# Command description for MoveWithOnOff
matter.send_command:
  path: some_endpoint.level_control.move_with_on_off
  arguments:
    move_mode: # enum: up, down
    rate:
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# Command description for StepWithOnOff
matter.send_command:
  path: some_endpoint.level_control.step_with_on_off
  arguments:
    step_mode: # enum: up, down
    step_size:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# Command description for StopWithOnOff
matter.send_command:
  path: some_endpoint.level_control.stop_with_on_off
  arguments:
    # options_mask: # bitmap: execute_if_off, couple_color_temp_to_level
    # options_override: # bitmap: execute_if_off, couple_color_temp_to_level

# This command will cause the device to change the current frequency to the requested value.
matter.send_command:
  path: some_endpoint.level_control.move_to_closest_frequency
  arguments:
    frequency:
```

# AccessControl

The Access Control Cluster exposes a data model view of a
Node's Access Control List (ACL), which codifies the rules used to manage
and enforce Access Control for the Node's endpoints and their associated
cluster instances.

```yaml
# This command signals to the service associated with the device vendor that the fabric administrator would like a review of the current restrictions on the accessing fabric.
matter.send_command:
  path: some_endpoint.access_control.review_fabric_restrictions
  arguments:
    arl:
```

# Actions

This cluster provides a standardized way for a Node (typically a Bridge, but could be any Node) to expose action information.

```yaml
# This command is used to trigger an instantaneous action.
matter.send_command:
  path: some_endpoint.actions.instant_action
  arguments:
    action_id:
    # invoke_id:

# This command is used to trigger an instantaneous action with a transition over a given time.
matter.send_command:
  path: some_endpoint.actions.instant_action_with_transition
  arguments:
    action_id:
    # invoke_id:
    transition_time:

# This command is used to trigger the commencement of an action.
matter.send_command:
  path: some_endpoint.actions.start_action
  arguments:
    action_id:
    # invoke_id:

# This command is used to trigger the commencement of an action with a duration.
matter.send_command:
  path: some_endpoint.actions.start_action_with_duration
  arguments:
    action_id:
    # invoke_id:
    duration:

# This command is used to stop an action.
matter.send_command:
  path: some_endpoint.actions.stop_action
  arguments:
    action_id:
    # invoke_id:

# This command is used to pause an action.
matter.send_command:
  path: some_endpoint.actions.pause_action
  arguments:
    action_id:
    # invoke_id:

# This command is used to pause an action with a duration.
matter.send_command:
  path: some_endpoint.actions.pause_action_with_duration
  arguments:
    action_id:
    # invoke_id:
    duration:

# This command is used to resume an action.
matter.send_command:
  path: some_endpoint.actions.resume_action
  arguments:
    action_id:
    # invoke_id:

# This command is used to enable an action.
matter.send_command:
  path: some_endpoint.actions.enable_action
  arguments:
    action_id:
    # invoke_id:

# This command is used to enable an action with a duration.
matter.send_command:
  path: some_endpoint.actions.enable_action_with_duration
  arguments:
    action_id:
    # invoke_id:
    duration:

# This command is used to disable an action.
matter.send_command:
  path: some_endpoint.actions.disable_action
  arguments:
    action_id:
    # invoke_id:

# This command is used to disable an action with a duration.
matter.send_command:
  path: some_endpoint.actions.disable_action_with_duration
  arguments:
    action_id:
    # invoke_id:
    duration:
```

# OTASoftwareUpdateProvider

This cluster implements the Provider role in the OTA process.

```yaml
# Upon receipt, this command SHALL trigger an attempt to find an updated Software Image by the OTA Provider to match the OTA Requestor's constraints provided in the payload fields.
matter.send_command:
  path: some_endpoint.ota_software_update_provider.query_image
  arguments:
    vendor_id:
    product_id:
    software_version:
    protocols_supported: # enum: bdx_synchronous, bdx_asynchronous, https, vendor_specific
    # hardware_version:
    # location:
    # requestor_can_consent: # default: 0
    # metadata_for_provider:

# This command requests the specified version be installed on the device.
matter.send_command:
  path: some_endpoint.ota_software_update_provider.apply_update_request
  arguments:
    update_token:
    new_version:

# This command tells the Provider that the specified update has been applied.
matter.send_command:
  path: some_endpoint.ota_software_update_provider.notify_update_applied
  arguments:
    update_token:
    software_version:
```

# OTASoftwareUpdateRequestor

This cluster implements the Requestor role in the OTA process.

```yaml
# This command MAY be invoked by Administrators to announce the presence of a particular OTA Provider.
matter.send_command:
  path: some_endpoint.ota_software_update_requestor.announce_ota_provider
  arguments:
    provider_node_id:
    vendor_id:
    announcement_reason: # enum: simple_announcement, update_available, urgent_update_available
    # metadata_for_node:
    endpoint:
```

# GeneralCommissioning

This cluster is used to manage global aspects of the Commissioning flow.

```yaml
# This command is used to arm or disarm the fail-safe timer.
matter.send_command:
  path: some_endpoint.general_commissioning.arm_fail_safe
  arguments:
    # expiry_length_seconds: # default: 900
    breadcrumb:

# This command is used to set the regulatory configuration for the device.
matter.send_command:
  path: some_endpoint.general_commissioning.set_regulatory_config
  arguments:
    new_regulatory_config: # enum: indoor, outdoor, indoor_outdoor
    country_code:
    breadcrumb:

# This command is used to indicate that the commissioning process is complete.
matter.send_command: some_endpoint.general_commissioning.commissioning_complete

# This command is used to set the user acknowledgements received in the Enhanced Setup Flow Terms & Conditions into the node.
matter.send_command:
  path: some_endpoint.general_commissioning.set_tc_acknowledgements
  arguments:
    tc_version:
    tc_user_response:
```

# NetworkCommissioning

Functionality to configure, enable, disable network credentials and access on a Matter device.

```yaml
# This command is used to scan for available networks on the network interface associated with the cluster instance.
matter.send_command:
  path: some_endpoint.network_commissioning.scan_networks
  arguments:
    # ssid:
    # breadcrumb:

# This command is used to add or update a Wi-Fi network configuration.
matter.send_command:
  path: some_endpoint.network_commissioning.add_or_update_wi_fi_network
  arguments:
    ssid:
    credentials:
    # breadcrumb:

# This command is used to add or update a Thread network configuration.
matter.send_command:
  path: some_endpoint.network_commissioning.add_or_update_thread_network
  arguments:
    operational_dataset:
    # breadcrumb:

# This command is used to remove a network configuration on the network interface associated with the cluster instance.
matter.send_command:
  path: some_endpoint.network_commissioning.remove_network
  arguments:
    network_id:
    # breadcrumb:

# This command is used to connect to a network on the network interface associated with the cluster instance.
matter.send_command:
  path: some_endpoint.network_commissioning.connect_network
  arguments:
    network_id:
    # breadcrumb:

# This command is used to re-order the network configuration list.
matter.send_command:
  path: some_endpoint.network_commissioning.reorder_network
  arguments:
    network_id:
    network_index:
    # breadcrumb:
```

# DiagnosticLogs

The cluster provides commands for retrieving unstructured diagnostic logs from a Node that may be used to aid in diagnostics.

```yaml
# Reception of this command starts the process of retrieving diagnostic logs from a Node.
matter.send_command:
  path: some_endpoint.diagnostic_logs.retrieve_logs_request
  arguments:
    intent: # enum: end_user_support, network_diag, crash_logs
    requested_protocol: # enum: response_payload, bdx
    # transfer_file_designator:
```

# GeneralDiagnostics

The General Diagnostics Cluster, along with other diagnostics clusters, provide a means to acquire standardized diagnostics metrics that MAY be used by a Node to assist a user or Administrative Node in diagnosing potential problems.

```yaml
# This command SHALL be supported to provide a means for certification tests to trigger some test-plan-specific events, necessary to assist in automation of device interactions for some certification test cases.
matter.send_command:
  path: some_endpoint.general_diagnostics.test_event_trigger
  arguments:
    enable_key:
    event_trigger:

# This command MAY be used by a client to obtain a correlated view of both System Time, and, if currently synchronized and supported, "wall clock time" of the server.
matter.send_command: some_endpoint.general_diagnostics.time_snapshot

# This command provides a means for certification tests or manufacturer's internal tests to validate particular command handling and encoding constraints by generating a response of a given size.
matter.send_command:
  path: some_endpoint.general_diagnostics.payload_test_request
  arguments:
    enable_key:
    value:
    count:
```

# SoftwareDiagnostics

The Software Diagnostics Cluster provides a means to acquire standardized diagnostics metrics that MAY be used by a Node to assist a user or Administrative Node in diagnosing potential problems.

```yaml
# This command is used to reset the high watermarks for heap and stack memory.
matter.send_command: some_endpoint.software_diagnostics.reset_watermarks
```

# ThreadNetworkDiagnostics

The Thread Network Diagnostics Cluster provides a means to acquire standardized diagnostics metrics that MAY be used by a Node to assist a user or Administrative Node in diagnosing potential problems

```yaml
# Reception of this command SHALL reset the following attributes to 0:
matter.send_command: some_endpoint.thread_network_diagnostics.reset_counts
```

# WiFiNetworkDiagnostics

The Wi-Fi Network Diagnostics Cluster provides a means to acquire standardized diagnostics metrics that MAY be used by a Node to assist a user or Administrative Node in diagnosing potential problems.

```yaml
# This command is used to reset the count attributes.
matter.send_command: some_endpoint.wi_fi_network_diagnostics.reset_counts
```

# EthernetNetworkDiagnostics

The Ethernet Network Diagnostics Cluster provides a means to acquire standardized diagnostics metrics that MAY be used by a Node to assist a user or Administrative Node in diagnosing potential problems.

```yaml
# This command is used to reset the count attributes.
matter.send_command: some_endpoint.ethernet_network_diagnostics.reset_counts
```

# TimeSynchronization

Accurate time is required for a number of reasons, including scheduling, display and validating security materials.

```yaml
# This command is used to set the UTC time of the node.
matter.send_command:
  path: some_endpoint.time_synchronization.set_utc_time
  arguments:
    # utc_time: # default: 0
    # granularity: # default: 0x00
    # time_source: # default: 0x00

# This command is used to set the TrustedTimeSource attribute.
matter.send_command:
  path: some_endpoint.time_synchronization.set_trusted_time_source
  arguments:
    trusted_time_source:

# This command is used to set the time zone of the node.
matter.send_command:
  path: some_endpoint.time_synchronization.set_time_zone
  arguments:
    time_zone:

# This command is used to set the DST offsets for a node.
matter.send_command:
  path: some_endpoint.time_synchronization.set_dst_offset
  arguments:
    dst_offset:

# This command is used to set the DefaultNTP attribute.
matter.send_command:
  path: some_endpoint.time_synchronization.set_default_ntp
  arguments:
    default_ntp:
```

# BridgedDeviceBasicInformation

This Cluster serves two purposes towards a Node communicating with a Bridge: indicate that the functionality on
the Endpoint where it is placed (and its Parts) is bridged from a non-CHIP technology; and provide a centralized
collection of attributes that the Node MAY collect to aid in conveying information regarding the Bridged Device to a user,
such as the vendor name, the model name, or user-assigned name.

```yaml
# Upon receipt, the server SHALL attempt to keep the bridged device active for the duration specified by the command, when the device is next active.
matter.send_command:
  path: some_endpoint.bridged_device_basic_information.keep_active
  arguments:
    stay_active_duration:
    timeout_ms:
```

# AdministratorCommissioning

Commands to trigger a Node to allow a new Administrator to commission it.

```yaml
# This command is used by a current Administrator to instruct a Node to go into commissioning mode.
matter.send_command:
  path: some_endpoint.administrator_commissioning.open_commissioning_window
  arguments:
    commissioning_timeout:
    pake_passcode_verifier:
    discriminator:
    iterations:
    salt:

# This command MAY be used by a current Administrator to instruct a Node to go into commissioning mode, if the node supports the Basic Commissioning Method.
matter.send_command:
  path: some_endpoint.administrator_commissioning.open_basic_commissioning_window
  arguments:
    commissioning_timeout:

# This command is used by a current Administrator to instruct a Node to revoke any active OpenCommissioningWindow or OpenBasicCommissioningWindow command.
matter.send_command: some_endpoint.administrator_commissioning.revoke_commissioning
```

# OperationalCredentials

This cluster is used to add or remove Operational Credentials on a Commissionee or Node, as well as manage the associated Fabrics.

```yaml
# This command is used to perform an attestation request.
matter.send_command:
  path: some_endpoint.operational_credentials.attestation_request
  arguments:
    attestation_nonce:

# This command is used to request a certificate from the device attestation certificate chain.
matter.send_command:
  path: some_endpoint.operational_credentials.certificate_chain_request
  arguments:
    certificate_type: # enum: dac_certificate, pai_certificate

# This command is used to perform a CSR request.
matter.send_command:
  path: some_endpoint.operational_credentials.csr_request
  arguments:
    csr_nonce:
    # is_for_update_noc: # default: 0

# This command is used to add a new NOC to the device.
matter.send_command:
  path: some_endpoint.operational_credentials.add_noc
  arguments:
    noc_value:
    # icac_value:
    ipk_value:
    case_admin_subject:
    admin_vendor_id:

# This command is used to update an existing NOC on the device.
matter.send_command:
  path: some_endpoint.operational_credentials.update_noc
  arguments:
    noc_value:
    # icac_value:

# This command is used to set the user-visible fabric label for a given Fabric.
matter.send_command:
  path: some_endpoint.operational_credentials.update_fabric_label
  arguments:
    label:

# This command is used to remove a Fabric from the device.
matter.send_command:
  path: some_endpoint.operational_credentials.remove_fabric
  arguments:
    fabric_index:

# This command is used to add a trusted root certificate to the device.
matter.send_command:
  path: some_endpoint.operational_credentials.add_trusted_root_certificate
  arguments:
    root_ca_certificate:

# This command is used to manage the VendorID and VIDVerificationStatement fields of the Fabrics attribute, and the VVSC field of an entry in the NOCs attribute.
matter.send_command:
  path: some_endpoint.operational_credentials.set_vid_verification_statement
  arguments:
    # vendor_id:
    # vid_verification_statement:
    # vvsc:

# This command is used to authenticate the fabric associated with the FabricIndex.
matter.send_command:
  path: some_endpoint.operational_credentials.sign_vid_verification_request
  arguments:
    fabric_index:
    client_challenge:
```

# GroupKeyManagement

The Group Key Management Cluster is the mechanism by which group keys are managed.

```yaml
# This command is used by Administrators to set the state of a given Group Key Set, including atomically updating the state of all epoch keys.
matter.send_command:
  path: some_endpoint.group_key_management.key_set_write
  arguments:
    group_key_set:

# This command is used by Administrators to read the state of a given Group Key Set.
matter.send_command:
  path: some_endpoint.group_key_management.key_set_read
  arguments:
    group_key_set_id:

# This command is used by Administrators to remove all state of a given Group Key Set.
matter.send_command:
  path: some_endpoint.group_key_management.key_set_remove
  arguments:
    group_key_set_id:

# This command is used by Administrators to query a list of all Group Key Sets associated with the accessing fabric.
matter.send_command: some_endpoint.group_key_management.key_set_read_all_indices
```

# ICDManagement

Allows servers to ensure that listed clients are notified when a server is available for communication.

```yaml
# This command allows a client to register itself with the ICD to be notified when the device is available for communication.
matter.send_command:
  path: some_endpoint.icd_management.register_client
  arguments:
    check_in_node_id:
    monitored_subject:
    key:
    # verification_key:
    client_type: # enum: permanent, ephemeral

# This command allows a client to unregister itself with the ICD.
matter.send_command:
  path: some_endpoint.icd_management.unregister_client
  arguments:
    check_in_node_id:
    # verification_key:

# This command allows a client to request that the server stays in active mode for at least a given time duration (in milliseconds) from when this command is received.
matter.send_command:
  path: some_endpoint.icd_management.stay_active_request
  arguments:
    stay_active_duration:
```

# OvenCavityOperationalState

This cluster supports remotely monitoring and, where supported, changing the operational state of an Oven.

```yaml
# This command SHALL be supported if the device supports remotely stopping the operation.
matter.send_command: some_endpoint.oven_cavity_operational_state.stop

# This command SHALL be supported if the device supports remotely starting the operation.
matter.send_command: some_endpoint.oven_cavity_operational_state.start
```

# OvenMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.oven_mode.change_to_mode
  arguments:
    new_mode:
```

# ModeSelect

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# On receipt of this command, if the NewMode field indicates a valid mode transition within the supported list, the server SHALL set the CurrentMode attribute to the NewMode value, otherwise, the server SHALL respond with an INVALID_COMMAND status response.
matter.send_command:
  path: some_endpoint.mode_select.change_to_mode
  arguments:
    new_mode:
```

# LaundryWasherMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.laundry_washer_mode.change_to_mode
  arguments:
    new_mode:
```

# RefrigeratorAndTemperatureControlledCabinetMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.refrigerator_and_temperature_controlled_cabinet_mode.change_to_mode
  arguments:
    new_mode:
```

# RVCRunMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.rvc_run_mode.change_to_mode
  arguments:
    new_mode:
```

# RVCCleanMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.rvc_clean_mode.change_to_mode
  arguments:
    new_mode:
```

# TemperatureControl

Attributes and commands for configuring the temperature control, and reporting temperature.

```yaml
# The SetTemperature command SHALL have the following data fields:
matter.send_command:
  path: some_endpoint.temperature_control.set_temperature
  arguments:
    # target_temperature:
    # target_temperature_level:
```

# DishwasherMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.dishwasher_mode.change_to_mode
  arguments:
    new_mode:
```

# SmokeCOAlarm

This cluster provides an interface for observing and managing the state of smoke and CO alarms.

```yaml
# This command SHALL initiate a device self-test.
matter.send_command: some_endpoint.smoke_co_alarm.self_test_request
```

# DishwasherAlarm

Attributes and commands for configuring the Dishwasher alarm.

```yaml
# This command resets active and latched alarms (if possible).
matter.send_command:
  path: some_endpoint.dishwasher_alarm.reset
  arguments:
    # alarms: # default: 0x00000000

# This command allows a client to request that an alarm be enabled or suppressed at the server.
matter.send_command:
  path: some_endpoint.dishwasher_alarm.modify_enabled_alarms
  arguments:
    # mask: # default: 0x00000000
```

# MicrowaveOvenControl

Attributes and commands for configuring the microwave oven control, and reporting cooking stats.

```yaml
# This command is used to set the cooking parameters associated with the operation of the device.
matter.send_command:
  path: some_endpoint.microwave_oven_control.set_cooking_parameters
  arguments:
    # cook_mode:
    # cook_time: # default: 30
    # power_setting: # default: 100
    # watt_setting_index:
    # start_after_setting: # default: 0

# This command is used to add more time to the CookTime attribute of the server.
matter.send_command:
  path: some_endpoint.microwave_oven_control.add_more_time
  arguments:
    time_to_add:
```

# OperationalState

This cluster supports remotely monitoring and, where supported, changing the operational state of any device where a state machine is a part of the operation.

```yaml
# This command SHALL be supported if the device supports remotely pausing the operation.
matter.send_command: some_endpoint.operational_state.pause

# This command SHALL be supported if the device supports remotely stopping the operation.
matter.send_command: some_endpoint.operational_state.stop

# This command SHALL be supported if the device supports remotely starting the operation.
matter.send_command: some_endpoint.operational_state.start

# This command SHALL be supported if the device supports remotely resuming the operation.
matter.send_command: some_endpoint.operational_state.resume
```

# RVCOperationalState

This cluster supports remotely monitoring and, where supported, changing the operational state of a Robotic Vacuum.

```yaml
# This command SHALL be supported if the device supports remotely pausing the operation.
matter.send_command: some_endpoint.rvc_operational_state.pause

# This command SHALL be supported if the device supports remotely resuming the operation.
matter.send_command: some_endpoint.rvc_operational_state.resume

# On receipt of this command, the device SHALL start seeking the charging dock, if possible in the current state of the device.
matter.send_command: some_endpoint.rvc_operational_state.go_home
```

# ScenesManagement

Attributes and commands for scene configuration and manipulation.

```yaml
# Add a scene to the scene table. Extension field sets are input as '{"ClusterID": VALUE, "AttributeValueList":[{"AttributeID": VALUE, "Value*": VALUE}]}'.
matter.send_command:
  path: some_endpoint.scenes_management.add_scene
  arguments:
    group_id:
    scene_id:
    transition_time:
    scene_name:
    extension_field_set_structs:

# Retrieves the requested scene entry from its Scene table.
matter.send_command:
  path: some_endpoint.scenes_management.view_scene
  arguments:
    group_id:
    scene_id:

# Removes the requested scene entry, corresponding to the value of the GroupID field, from its Scene Table
matter.send_command:
  path: some_endpoint.scenes_management.remove_scene
  arguments:
    group_id:
    scene_id:

# Remove all scenes, corresponding to the value of the GroupID field, from its Scene Table
matter.send_command:
  path: some_endpoint.scenes_management.remove_all_scenes
  arguments:
    group_id:

# Adds the scene entry into its Scene Table along with all extension field sets corresponding to the current state of other clusters on the same endpoint
matter.send_command:
  path: some_endpoint.scenes_management.store_scene
  arguments:
    group_id:
    scene_id:

# Set the attributes and corresponding state for each other cluster implemented on the endpoint accordingly to the resquested scene entry in the Scene Table
matter.send_command:
  path: some_endpoint.scenes_management.recall_scene
  arguments:
    group_id:
    scene_id:
    # transition_time:

# This command can be used to get the used scene identifiers within a certain group, for the endpoint that implements this cluster.
matter.send_command:
  path: some_endpoint.scenes_management.get_scene_membership
  arguments:
    group_id:

# This command allows a client to efficiently copy scenes from one group/scene identifier pair to another group/scene identifier pair.
matter.send_command:
  path: some_endpoint.scenes_management.copy_scene
  arguments:
    # mode: # bitmap: copy_all_scenes
    group_identifier_from:
    scene_identifier_from:
    group_identifier_to:
    scene_identifier_to:
```

# Groupcast

The Groupcast cluster manages the content of the node-wide multicast Group membership that is part of the underlying interaction layer.

```yaml
# This command SHALL be used to instruct the server to join a multicast group.
matter.send_command:
  path: some_endpoint.groupcast.join_group
  arguments:
    group_id:
    endpoints:
    key_set_id:
    # key:
    # use_auxiliary_acl:
    # replace_endpoints:
    # mcast_addr_policy: # enum: iana_addr, per_group

# This command SHALL allow a maintainer to request that the server withdraws itself or specific endpoints from a specific group or from all groups of this client's fabric.
matter.send_command:
  path: some_endpoint.groupcast.leave_group
  arguments:
    group_id:
    # endpoints:

# This command SHALL allow a fabric administrator to update the OperationalGroupKey associated with the existing group identified by GroupID, which is already joined.
matter.send_command:
  path: some_endpoint.groupcast.update_group_key
  arguments:
    group_id:
    key_set_id:
    # key:

# This command SHALL allow an Administrator to enable or disable the generation of AuxiliaryACL entries in the Access Control Cluster based on the groups joined (see Groupcast Auxiliary ACL Handling).
matter.send_command:
  path: some_endpoint.groupcast.configure_auxiliary_acl
  arguments:
    group_id:
    use_auxiliary_acl:

# This command SHALL allow an Administrator to configure test modes that allow validation of Groupcast communication.
matter.send_command:
  path: some_endpoint.groupcast.groupcast_testing
  arguments:
    test_operation: # enum: disable_testing, enable_listener_testing, enable_sender_testing
    # duration_seconds: # default: 60
```

# HEPAFilterMonitoring

Attributes and commands for monitoring HEPA filters in a device

```yaml
# Upon receipt, the device SHALL reset the Condition and ChangeIndicator attributes, indicating full resource availability and readiness for use, as initially configured.
matter.send_command: some_endpoint.hepa_filter_monitoring.reset_condition
```

# ActivatedCarbonFilterMonitoring

Attributes and commands for monitoring activated carbon filters in a device

```yaml
# Upon receipt, the device SHALL reset the Condition and ChangeIndicator attributes, indicating full resource availability and readiness for use, as initially configured.
matter.send_command: some_endpoint.activated_carbon_filter_monitoring.reset_condition
```

# WaterTankLevelMonitoring

This generic cluster provides an interface to the current condition of a resource.

```yaml
# Upon receipt, the device SHALL reset the Condition and ChangeIndicator attributes, indicating full resource availability and readiness for use, as initially configured.
matter.send_command: some_endpoint.water_tank_level_monitoring.reset_condition
```

# BooleanStateConfiguration

This cluster is used to configure a boolean sensor.

```yaml
# This command will suppress the alarm modes specified.
matter.send_command:
  path: some_endpoint.boolean_state_configuration.suppress_alarm
  arguments:
    # alarms_to_suppress: # bitmap: visual, audible

# This command allows setting the enable or disable value for the specified alarm modes.
matter.send_command:
  path: some_endpoint.boolean_state_configuration.enable_disable_alarm
  arguments:
    # alarms_to_enable_disable: # bitmap: visual, audible
```

# ValveConfigurationandControl

This cluster is used to configure a valve.

```yaml
# This command is used to set the valve to its open position.
matter.send_command:
  path: some_endpoint.valve_configurationand_control.open
  arguments:
    # open_duration:
    # target_level:

# This command is used to set the valve to its closed position.
matter.send_command: some_endpoint.valve_configurationand_control.close
```

# WaterHeaterManagement

This cluster is used to allow clients to control the operation of a hot water heating appliance so that it can be used with energy management.

```yaml
# Allows a client to request that the water heater is put into a Boost state.
matter.send_command:
  path: some_endpoint.water_heater_management.boost
  arguments:
    boost_info:

# Allows a client to cancel an ongoing Boost operation.
matter.send_command: some_endpoint.water_heater_management.cancel_boost
```

# CommodityPrice

The Commodity Price Cluster provides the mechanism for communicating Gas, Energy, or Water pricing information within the premises.

```yaml
# Upon receipt, this SHALL generate a GetDetailedPrice Response command.
matter.send_command:
  path: some_endpoint.commodity_price.get_detailed_price_request
  arguments:
    # details: # bitmap: description, components

# Upon receipt, this SHALL generate a GetDetailedForecast Response command.
matter.send_command:
  path: some_endpoint.commodity_price.get_detailed_forecast_request
  arguments:
    # details: # bitmap: description, components
```

# Messages

This cluster provides an interface for passing messages to be presented by a device.

```yaml
# Upon receipt, this SHALL cause the message in the passed fields to be appended to the Messages attribute.
matter.send_command:
  path: some_endpoint.messages.present_messages_request
  arguments:
    message_id:
    priority: # enum: low, medium, high, critical
    # message_control: # bitmap: confirmation_required, response_required, reply_message, message_confirmed, message_protected
    start_time:
    duration:
    message_text:
    # responses:

# This command will cancel the message IDs specified.
matter.send_command:
  path: some_endpoint.messages.cancel_messages_request
  arguments:
    message_i_ds:
```

# DeviceEnergyManagement

This cluster allows a client to manage the power draw of a device. An example of such a client could be an Energy Management System (EMS) which controls an Energy Smart Appliance (ESA).

```yaml
# Allows a client to request an adjustment in the power consumption of an ESA for a specified duration.
matter.send_command:
  path: some_endpoint.device_energy_management.power_adjust_request
  arguments:
    power:
    duration:
    cause: # enum: local_optimization, grid_optimization

# Allows a client to cancel an ongoing PowerAdjustmentRequest operation.
matter.send_command: some_endpoint.device_energy_management.cancel_power_adjust_request

# Allows a client to adjust the start time of a Forecast sequence that has not yet started operation (i.e. where the current Forecast StartTime is in the future).
matter.send_command:
  path: some_endpoint.device_energy_management.start_time_adjust_request
  arguments:
    requested_start_time:
    cause: # enum: local_optimization, grid_optimization

# Allows a client to temporarily pause an operation and reduce the ESAs energy demand.
matter.send_command:
  path: some_endpoint.device_energy_management.pause_request
  arguments:
    duration:
    cause: # enum: local_optimization, grid_optimization

# Allows a client to cancel the PauseRequest command and enable earlier resumption of operation.
matter.send_command: some_endpoint.device_energy_management.resume_request

# Allows a client to modify a Forecast within the limits allowed by the ESA.
matter.send_command:
  path: some_endpoint.device_energy_management.modify_forecast_request
  arguments:
    forecast_id:
    slot_adjustments:
    cause: # enum: local_optimization, grid_optimization

# Allows a client to ask the ESA to recompute its Forecast based on power and time constraints.
matter.send_command:
  path: some_endpoint.device_energy_management.request_constraint_based_forecast
  arguments:
    constraints:
    cause: # enum: local_optimization, grid_optimization

# Allows a client to request cancellation of a previous adjustment request in a StartTimeAdjustRequest, ModifyForecastRequest or RequestConstraintBasedForecast command.
matter.send_command: some_endpoint.device_energy_management.cancel_request

# Allows a client to request an adjustment in the range of powers that an ESA can operate within for a specified duration.
matter.send_command:
  path: some_endpoint.device_energy_management.power_range_adjust_request
  arguments:
    # min_power: # default: 0
    # max_power: # default: 4611686018427387904
    duration:
    cause: # enum: local_optimization, grid_optimization

# Allows a client to cancel an ongoing PowerRangeAdjustmentRequest operation.
matter.send_command: some_endpoint.device_energy_management.cancel_power_range_adjust_request
```

# EnergyEVSE

Electric Vehicle Supply Equipment (EVSE) is equipment used to charge an Electric Vehicle (EV) or Plug-In Hybrid Electric Vehicle. This cluster provides an interface to the functionality of Electric Vehicle Supply Equipment (EVSE) management.

```yaml
# Allows a client to disable the EVSE from charging and discharging.
matter.send_command: some_endpoint.energy_evse.disable

# This command allows a client to enable the EVSE to charge an EV, and to provide or update the maximum and minimum charge current.
matter.send_command:
  path: some_endpoint.energy_evse.enable_charging
  arguments:
    charging_enabled_until:
    minimum_charge_current:
    maximum_charge_current:

# Upon receipt, this SHALL allow a client to enable the discharge of an EV, and to provide or update the maximum discharge current.
matter.send_command:
  path: some_endpoint.energy_evse.enable_discharging
  arguments:
    discharging_enabled_until:
    maximum_discharge_current:

# Allows a client to put the EVSE into a self-diagnostics mode.
matter.send_command: some_endpoint.energy_evse.start_diagnostics

# Allows a client to set the user specified charging targets.
matter.send_command:
  path: some_endpoint.energy_evse.set_targets
  arguments:
    charging_target_schedules:

# Allows a client to retrieve the current set of charging targets.
matter.send_command: some_endpoint.energy_evse.get_targets

# Allows a client to clear all stored charging targets.
matter.send_command: some_endpoint.energy_evse.clear_targets
```

# EnergyEVSEMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.energy_evse_mode.change_to_mode
  arguments:
    new_mode:
```

# WaterHeaterMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.water_heater_mode.change_to_mode
  arguments:
    new_mode:
```

# DeviceEnergyManagementMode

Attributes and commands for selecting a mode from a list of supported options.

```yaml
# This command is used to change device modes.
matter.send_command:
  path: some_endpoint.device_energy_management_mode.change_to_mode
  arguments:
    new_mode:
```

# ElectricalAlarm

This cluster is a derived cluster of the Alarm Base cluster and provides the alarm definition related to Electrical Sensor device type.

```yaml
# This command resets active and latched alarms (if possible).
matter.send_command:
  path: some_endpoint.electrical_alarm.reset
  arguments:
    # alarms: # bitmap: over_voltage, under_voltage, swapped_polarity, loss_of_measurement, loss_of_grid_power, power_imported, power_exported, over_frequency, under_frequency, over_power, under_power, over_current, under_current, frequency_quality, voltage_quality

# This command allows a client to request that an alarm be enabled or suppressed at the server.
matter.send_command:
  path: some_endpoint.electrical_alarm.modify_enabled_alarms
  arguments:
    # mask: # bitmap: over_voltage, under_voltage, swapped_polarity, loss_of_measurement, loss_of_grid_power, power_imported, power_exported, over_frequency, under_frequency, over_power, under_power, over_current, under_current, frequency_quality, voltage_quality

# This command will set the alarm thresholds for the specified values.
matter.send_command:
  path: some_endpoint.electrical_alarm.set_electrical_alarm_thresholds
  arguments:
    # over_voltage_threshold: # default: 4611686018427387904
    # under_voltage_threshold: # default: 0
    # over_frequency_threshold: # default: 1000000
    # under_frequency_threshold: # default: 0
    # over_power_threshold: # default: 4611686018427387904
    # under_power_threshold: # default: -4611686018427387904
    # over_current_threshold: # default: 4611686018427387904
    # under_current_threshold: # default: -4611686018427387904
    # power_import_threshold: # default: 4611686018427387904
    # power_export_threshold: # default: -4611686018427387904
```

# ElectricalProtectionAlarm

This cluster is a derived cluster of the Alarm Base cluster and provides the alarm definition related to the Electrical Protection device type.

```yaml
# This command allows a client to request that an alarm be enabled or suppressed at the server.
matter.send_command:
  path: some_endpoint.electrical_protection_alarm.modify_enabled_alarms
  arguments:
    # mask: # bitmap: short_circuit_fault, over_load_fault, over_voltage_fault, voltage_surge_fault, residual_current_fault, arc_fault, self_test
```

# DoorLock

An interface to a generic way to secure a door

```yaml
# This command causes the lock device to lock the door.
matter.send_command:
  path: some_endpoint.door_lock.lock_door
  arguments:
    # pin_code:

# This command causes the lock device to unlock the door.
matter.send_command:
  path: some_endpoint.door_lock.unlock_door
  arguments:
    # pin_code:

# This command causes the lock device to unlock the door with a timeout parameter.
matter.send_command:
  path: some_endpoint.door_lock.unlock_with_timeout
  arguments:
    timeout:
    # pin_code:

# Set a weekly repeating schedule for a specified user.
matter.send_command:
  path: some_endpoint.door_lock.set_week_day_schedule
  arguments:
    week_day_index:
    user_index:
    # days_mask: # bitmap: sunday, monday, tuesday, wednesday, thursday, friday, saturday
    start_hour:
    start_minute:
    end_hour:
    end_minute:

# Retrieve the specific weekly schedule for the specific user.
matter.send_command:
  path: some_endpoint.door_lock.get_week_day_schedule
  arguments:
    week_day_index:
    user_index:

# Clear the specific weekly schedule or all weekly schedules for the specific user.
matter.send_command:
  path: some_endpoint.door_lock.clear_week_day_schedule
  arguments:
    week_day_index:
    user_index:

# Set a time-specific schedule ID for a specified user.
matter.send_command:
  path: some_endpoint.door_lock.set_year_day_schedule
  arguments:
    year_day_index:
    user_index:
    local_start_time:
    local_end_time:

# Retrieve the specific year day schedule for the specific schedule and user indexes.
matter.send_command:
  path: some_endpoint.door_lock.get_year_day_schedule
  arguments:
    year_day_index:
    user_index:

# Clears the specific year day schedule or all year day schedules for the specific user.
matter.send_command:
  path: some_endpoint.door_lock.clear_year_day_schedule
  arguments:
    year_day_index:
    user_index:

# Set the holiday Schedule by specifying local start time and local end time with respect to any Lock Operating Mode.
matter.send_command:
  path: some_endpoint.door_lock.set_holiday_schedule
  arguments:
    holiday_index:
    local_start_time:
    local_end_time:
    operating_mode: # enum: normal, vacation, privacy, no_remote_lock_unlock, passage

# Get the holiday schedule for the specified index.
matter.send_command:
  path: some_endpoint.door_lock.get_holiday_schedule
  arguments:
    holiday_index:

# Clears the holiday schedule or all holiday schedules.
matter.send_command:
  path: some_endpoint.door_lock.clear_holiday_schedule
  arguments:
    holiday_index:

# Set user into the lock.
matter.send_command:
  path: some_endpoint.door_lock.set_user
  arguments:
    operation_type: # enum: add, clear, modify
    user_index:
    user_name:
    user_unique_id:
    user_status: # enum: available, occupied_enabled, occupied_disabled
    user_type: # enum: unrestricted_user, year_day_schedule_user, week_day_schedule_user, programming_user, non_access_user, forced_user, disposable_user, expiring_user, schedule_restricted_user, remote_only_user
    credential_rule: # enum: single, dual, tri

# Retrieve user.
matter.send_command:
  path: some_endpoint.door_lock.get_user
  arguments:
    user_index:

# Clears a user or all Users.
matter.send_command:
  path: some_endpoint.door_lock.clear_user
  arguments:
    user_index:

# Set a credential (e.g. PIN, RFID, Fingerprint, etc.) into the lock for a new user, existing user, or ProgrammingUser.
matter.send_command:
  path: some_endpoint.door_lock.set_credential
  arguments:
    operation_type: # enum: add, clear, modify
    credential:
    credential_data:
    user_index:
    user_status: # enum: available, occupied_enabled, occupied_disabled
    user_type: # enum: unrestricted_user, year_day_schedule_user, week_day_schedule_user, programming_user, non_access_user, forced_user, disposable_user, expiring_user, schedule_restricted_user, remote_only_user

# Retrieve the status of a particular credential (e.g. PIN, RFID, Fingerprint, etc.) by index.
matter.send_command:
  path: some_endpoint.door_lock.get_credential_status
  arguments:
    credential:

# Clear one, one type, or all credentials except ProgrammingPIN credential.
matter.send_command:
  path: some_endpoint.door_lock.clear_credential
  arguments:
    credential:

# This command causes the lock device to unlock the door without pulling the latch.
matter.send_command:
  path: some_endpoint.door_lock.unbolt_door
  arguments:
    # pin_code:

# This command allows communicating an Aliro Reader configuration, as defined in Aliro, to the lock.
matter.send_command:
  path: some_endpoint.door_lock.set_aliro_reader_config
  arguments:
    signing_key:
    verification_key:
    group_identifier:
    # group_resolving_key:

# This command allows clearing an existing Aliro Reader configuration for the lock.
matter.send_command: some_endpoint.door_lock.clear_aliro_reader_config
```

# WindowCovering

Provides an interface for controlling and adjusting automatic window coverings.

```yaml
# Upon receipt of this command, the window covering will adjust its position so the physical lift/slide and tilt is at the maximum open/up position.
matter.send_command: some_endpoint.window_covering.up_or_open

# Upon receipt of this command, the window covering will adjust its position so the physical lift/slide and tilt is at the maximum closed/down position.
matter.send_command: some_endpoint.window_covering.down_or_close

# Upon receipt of this command, the window covering will stop any adjusting to the physical tilt and lift/slide that is currently occurring.
matter.send_command: some_endpoint.window_covering.stop_motion

# Go to lift value specified
matter.send_command:
  path: some_endpoint.window_covering.go_to_lift_value
  arguments:
    lift_value:

# This command is used to set the target lift position of the window covering to the percentage value specified in the command.
matter.send_command:
  path: some_endpoint.window_covering.go_to_lift_percentage
  arguments:
    lift_percent100ths_value:

# Go to tilt value specified
matter.send_command:
  path: some_endpoint.window_covering.go_to_tilt_value
  arguments:
    tilt_value:

# This command is used to set the target tilt position of the window covering to the percentage value specified in the command.
matter.send_command:
  path: some_endpoint.window_covering.go_to_tilt_percentage
  arguments:
    tilt_percent100ths_value:
```

# ClosureControl

This cluster provides an interface for controlling a Closure.

```yaml
# On receipt of this command, the closure SHALL stop its movement as fast as the closure is able too.
matter.send_command: some_endpoint.closure_control.stop

# On receipt of this command, the closure SHALL operate to update its position, latch state and/or motion speed.
matter.send_command:
  path: some_endpoint.closure_control.move_to
  arguments:
    # position: # enum: move_to_fully_closed, move_to_fully_open, move_to_pedestrian_position, move_to_ventilation_position, move_to_signature_position
    # latch:
    # speed: # default: 0x00

# This command is used to trigger a calibration of the closure.
matter.send_command: some_endpoint.closure_control.calibrate
```

# ClosureDimension

This cluster provides an interface to reflect and control a closure's range of movement, usually involving a panel, by using 6-axis framework.

```yaml
# This command is used to move a dimension of the closure to a target position.
matter.send_command:
  path: some_endpoint.closure_dimension.set_target
  arguments:
    # position:
    # latch:
    # speed: # default: 0x00

# This command is used to move a dimension of the closure to a target position by a number of steps.
matter.send_command:
  path: some_endpoint.closure_dimension.step
  arguments:
    direction: # enum: decrease, increase
    number_of_steps:
    # speed: # default: 0x00
```

# ServiceArea

The Service Area cluster provides an interface for controlling the areas where a device should operate, and for querying the current area being serviced.

```yaml
# This command is used to select a set of device areas, where the device is to operate.
matter.send_command:
  path: some_endpoint.service_area.select_areas
  arguments:
    new_areas:

# This command is used to skip the given area, and to attempt operating at other areas on the SupportedAreas attribute list.
matter.send_command:
  path: some_endpoint.service_area.skip_area
  arguments:
    skipped_area:
```

# Thermostat

An interface for configuring and controlling the functionality of a thermostat.

```yaml
# This command will raise or lower the setpoint based on the provided values.
matter.send_command:
  path: some_endpoint.thermostat.setpoint_raise_lower
  arguments:
    mode: # enum: heat, cool, both
    amount:

# This command is used to update the thermostat weekly setpoint schedule from a management system.
matter.send_command:
  path: some_endpoint.thermostat.set_weekly_schedule
  arguments:
    number_of_transitions_for_sequence:
    # day_of_week_for_sequence: # bitmap: sunday, monday, tuesday, wednesday, thursday, friday, saturday, away
    # mode_for_sequence: # bitmap: heat_setpoint_present, cool_setpoint_present
    transitions:

# The Current Weekly Schedule Command is sent from the server in response to the Get Weekly Schedule Command.
matter.send_command:
  path: some_endpoint.thermostat.get_weekly_schedule
  arguments:
    # days_to_return: # bitmap: sunday, monday, tuesday, wednesday, thursday, friday, saturday, away
    # mode_to_return: # bitmap: heat_setpoint_present, cool_setpoint_present

# This command is used to clear the weekly schedule.
matter.send_command: some_endpoint.thermostat.clear_weekly_schedule

# This command will set the active schedule to the provided schedule handle.
matter.send_command:
  path: some_endpoint.thermostat.set_active_schedule_request
  arguments:
    schedule_handle:

# This command will set the active preset to the provided preset handle.
matter.send_command:
  path: some_endpoint.thermostat.set_active_preset_request
  arguments:
    preset_handle:

# This command will add a new suggestion based on the specified values.
matter.send_command:
  path: some_endpoint.thermostat.add_thermostat_suggestion
  arguments:
    preset_handle:
    effective_time:
    expiration_in_minutes:

# This command will remove the specified suggestion.
matter.send_command:
  path: some_endpoint.thermostat.remove_thermostat_suggestion
  arguments:
    unique_id:

# Begins, Commits or Cancels an atomic write
matter.send_command:
  path: some_endpoint.thermostat.atomic_request
  arguments:
    request_type: # enum: begin_write, commit_write, rollback_write
    attribute_requests:
    # timeout:
```

# FanControl

An interface for controlling a fan in a heating/cooling system.

```yaml
# This command speeds up or slows down the fan, in steps, without a client having to know the fan speed.
matter.send_command:
  path: some_endpoint.fan_control.step
  arguments:
    # direction: # default: 0x00
    # wrap: # default: 0
    # lowest_off: # default: 1
```

# Humidistat

This cluster specifies an interface to control the humidity of an environment.

```yaml
# This command will set the specified settings to new values.
matter.send_command:
  path: some_endpoint.humidistat.set_settings
  arguments:
    # user_setpoint:
    # mode: # enum: off, humidifier, dehumidifier, auto, fan_only
    # mist_type: # bitmap: mist_cold, mist_warm
    # continuous:
    # sleep:
    # optimal:
```

# ColorControl

Attributes and commands for controlling the color properties of a color-capable light.

```yaml
# This command will move the device to the requested hue using a transition.
matter.send_command:
  path: some_endpoint.color_control.move_to_hue
  arguments:
    hue:
    direction: # enum: shortest, longest, up, down
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will change the hue of the device with a requested rate.
matter.send_command:
  path: some_endpoint.color_control.move_hue
  arguments:
    move_mode: # enum: stop, up, down
    rate:
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will change the hue of the device using a step and transition.
matter.send_command:
  path: some_endpoint.color_control.step_hue
  arguments:
    step_mode: # enum: up, down
    step_size:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will move the device to the requested saturation using a transition.
matter.send_command:
  path: some_endpoint.color_control.move_to_saturation
  arguments:
    saturation:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will change the saturation of the device with a requested rate.
matter.send_command:
  path: some_endpoint.color_control.move_saturation
  arguments:
    move_mode: # enum: stop, up, down
    rate:
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will change the saturation of the device using a step and transition.
matter.send_command:
  path: some_endpoint.color_control.step_saturation
  arguments:
    step_mode: # enum: up, down
    step_size:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will move the device to the requested hue and saturation using a transition.
matter.send_command:
  path: some_endpoint.color_control.move_to_hue_and_saturation
  arguments:
    hue:
    saturation:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will move the device to the requested color using a transition.
matter.send_command:
  path: some_endpoint.color_control.move_to_color
  arguments:
    color_x:
    color_y:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will change the color of the device with a requested rate.
matter.send_command:
  path: some_endpoint.color_control.move_color
  arguments:
    rate_x:
    rate_y:
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will change the color of the device using a step and transition.
matter.send_command:
  path: some_endpoint.color_control.step_color
  arguments:
    step_x:
    step_y:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command will move the device to the requested color temperate using a transition.
matter.send_command:
  path: some_endpoint.color_control.move_to_color_temperature
  arguments:
    color_temperature_mireds:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command allows the light to be moved in a smooth continuous transition from their current hue to a target hue.
matter.send_command:
  path: some_endpoint.color_control.enhanced_move_to_hue
  arguments:
    enhanced_hue:
    direction: # enum: shortest, longest, up, down
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command allows the light to start a continuous transition starting from their current hue.
matter.send_command:
  path: some_endpoint.color_control.enhanced_move_hue
  arguments:
    move_mode: # enum: stop, up, down
    rate:
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command allows the device to be moved in a stepped transition from their current hue.
matter.send_command:
  path: some_endpoint.color_control.enhanced_step_hue
  arguments:
    step_mode: # enum: up, down
    step_size:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command allows the light to be moved in a smooth continuous transition from their current hue to a target hue and from their current saturation to a target saturation.
matter.send_command:
  path: some_endpoint.color_control.enhanced_move_to_hue_and_saturation
  arguments:
    enhanced_hue:
    saturation:
    # transition_time: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command allows a color loop to be activated such that the color light cycles through its range of hues.
matter.send_command:
  path: some_endpoint.color_control.color_loop_set
  arguments:
    # update_flags: # bitmap: update_action, update_direction, update_time, update_start_hue
    action: # enum: deactivate, activate_from_color_loop_start_enhanced_hue, activate_from_enhanced_current_hue
    direction: # enum: decrement, increment
    time:
    start_hue:
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command is provided to allow MoveTo and Step commands to be stopped.
matter.send_command:
  path: some_endpoint.color_control.stop_move_step
  arguments:
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command allows the color temperature of the light to be moved at a specified rate.
matter.send_command:
  path: some_endpoint.color_control.move_color_temperature
  arguments:
    move_mode: # enum: stop, up, down
    rate:
    # color_temperature_minimum_mireds: # default: 0
    # color_temperature_maximum_mireds: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off

# This command allows the color temperature of the light to be stepped with a specified step size.
matter.send_command:
  path: some_endpoint.color_control.step_color_temperature
  arguments:
    step_mode: # enum: up, down
    step_size:
    # transition_time: # default: 0
    # color_temperature_minimum_mireds: # default: 0
    # color_temperature_maximum_mireds: # default: 0
    # options_mask: # bitmap: execute_if_off
    # options_override: # bitmap: execute_if_off
```

# DynamicLighting

This cluster provides a means to activate and control dynamic lighting effects on a light.

```yaml
# This command will start the specified effect.
matter.send_command:
  path: some_endpoint.dynamic_lighting.start_effect
  arguments:
    effect_id:
    speed:
    color_mode: # enum: level, xy, xy_and_level, hs, hs_and_level, ehue, ehue_and_level
    color_palette:

# This command will stop the current effect.
matter.send_command: some_endpoint.dynamic_lighting.stop_effect
```

# ProximityRanging

This cluster enables the configuration of proximity ranging sessions and reporting of proximity ranging data.

```yaml
# This command allows the Client to request the start of a ranging session.
matter.send_command:
  path: some_endpoint.proximity_ranging.start_ranging_request
  arguments:
    technology: # enum: bluetooth_channel_sounding, wi_fi_round_trip_time_ranging, wi_fi_next_generation_ranging, ble_beacon_rssi_ranging
    # wi_fi_ranging_device_role_config:
    # ble_ranging_device_role_config:
    # blt_channel_sounding_device_role_config:
    # frequency_band: # bitmap: 2_g4, 3_g65, 5_g, 6_g, 60_g, s1_g, 45_g
    # bandwidth: # bitmap: 1_m_hz, 2_m_hz, 4_m_hz, 8_m_hz, 16_m_hz, 20_m_hz, 40_m_hz, 80_m_hz, 160_m_hz, 320_m_hz
    security_mode: # enum: secure_ranging, open_ranging
    trigger:
    # reporting_condition:

# Upon receipt of a StopRangingRequest command that contains a SessionID that matches an active ranging session, the Server SHALL terminate the corresponding ranging session.
matter.send_command:
  path: some_endpoint.proximity_ranging.stop_ranging_request
  arguments:
    session_id:
```

# NetworkIdentityManagement

This cluster manages the identities and credentials of devices that connect to a managed network.

```yaml
# Adds an entry with the specified Client Identity to the Client Table.
matter.send_command:
  path: some_endpoint.network_identity_management.add_client
  arguments:
    client_identity:

# Removes the specified client from the Client Table and from the network.
matter.send_command:
  path: some_endpoint.network_identity_management.remove_client
  arguments:
    # client_index:
    # client_identifier:

# Resolves the full identity certificate for a Network Identity or Network Client Identity.
matter.send_command:
  path: some_endpoint.network_identity_management.query_identity
  arguments:
    # network_identity_index:
    # network_identity_type: # enum: ecdsa
    # client_index:
    # identifier:

# Imports a Network Administrator Shared Secret (NASS) and derives and activates the corresponding Network Identity for PDC authentication.
matter.send_command:
  path: some_endpoint.network_identity_management.import_admin_secret
  arguments:
    network_administrator_shared_secret:

# Exports the current Network Administrator Shared Secret.
matter.send_command: some_endpoint.network_identity_management.export_admin_secret
```

# WiFiNetworkManagement

Functionality to retrieve operational information about a managed Wi-Fi network.

```yaml
# This command is used to request the current WPA-Personal passphrase or PSK associated with the Wi-Fi network provided by this device.
matter.send_command: some_endpoint.wi_fi_network_management.network_passphrase_request
```

# ThreadBorderRouterManagement

Manage the Thread network of Thread Border Router

```yaml
# This command SHALL be used to request the active operational dataset of the Thread network to which the border router is connected.
matter.send_command: some_endpoint.thread_border_router_management.get_active_dataset_request

# This command SHALL be used to request the pending dataset of the Thread network to which the border router is connected.
matter.send_command: some_endpoint.thread_border_router_management.get_pending_dataset_request

# This command SHALL be used to set the active Dataset of the Thread network to which the Border Router is connected, when there is no active dataset already.
matter.send_command:
  path: some_endpoint.thread_border_router_management.set_active_dataset_request
  arguments:
    active_dataset:
    # breadcrumb:

# This command SHALL be used to set or update the pending Dataset of the Thread network to which the Border Router is connected, if the Border Router supports PANChange Feature.
matter.send_command:
  path: some_endpoint.thread_border_router_management.set_pending_dataset_request
  arguments:
    pending_dataset:
```

# ThreadNetworkDirectory

Manages the names and credentials of Thread networks visible to the user.

```yaml
# Adds an entry to the ThreadNetworks attribute with the specified Thread Operational Dataset.
matter.send_command:
  path: some_endpoint.thread_network_directory.add_network
  arguments:
    operational_dataset:

# Removes the network with the given Extended PAN ID from the ThreadNetworks attribute.
matter.send_command:
  path: some_endpoint.thread_network_directory.remove_network
  arguments:
    extended_pan_id:

# Retrieves the Thread Operational Dataset with the given Extended PAN ID.
matter.send_command:
  path: some_endpoint.thread_network_directory.get_operational_dataset
  arguments:
    extended_pan_id:
```

# Channel

This cluster provides an interface for controlling the current Channel on a device.

```yaml
# Change the channel on the media player to the channel case-insensitive exact matching the value passed as an argument.
matter.send_command:
  path: some_endpoint.channel.change_channel
  arguments:
    match:

# Change the channel on the media plaeyer to the channel with the given Number in the ChannelList attribute.
matter.send_command:
  path: some_endpoint.channel.change_channel_by_number
  arguments:
    major_number:
    minor_number:

# This command provides channel up and channel down functionality, but allows channel index jumps of size Count. When the value of the increase or decrease is larger than the number of channels remaining in the given direction, then the behavior SHALL be to return to the beginning (or end) of the channel list and continue. For example, if the current channel is at index 0 and count value of -1 is given, then the current channel should change to the last channel.
matter.send_command:
  path: some_endpoint.channel.skip_channel
  arguments:
    count:

# This command retrieves the program guide. It accepts several filter parameters to return specific schedule and program information from a content app. The command shall receive in response a ProgramGuideResponse.
matter.send_command:
  path: some_endpoint.channel.get_program_guide
  arguments:
    # start_time:
    # end_time:
    # channel_list:
    # page_token:
    # recording_flag: # bitmap: scheduled, record_series, recorded
    # external_id_list:
    # data:

# Record a specific program or series when it goes live. This functionality enables DVR recording features.
matter.send_command:
  path: some_endpoint.channel.record_program
  arguments:
    program_identifier:
    should_record_series:
    external_id_list:
    data:

# Cancel recording for a specific program or series.
matter.send_command:
  path: some_endpoint.channel.cancel_record_program
  arguments:
    program_identifier:
    should_record_series:
    external_id_list:
    data:
```

# TargetNavigator

This cluster provides an interface for UX navigation within a set of targets on a device or endpoint.

```yaml
# Upon receipt, this SHALL navigation the UX to the target identified.
matter.send_command:
  path: some_endpoint.target_navigator.navigate_target
  arguments:
    target:
    # data:
```

# MediaPlayback

This cluster provides an interface for controlling Media Playback (PLAY, PAUSE, etc) on a media device such as a TV or Speaker.

```yaml
# Upon receipt, this SHALL play media.
matter.send_command: some_endpoint.media_playback.play

# Upon receipt, this SHALL pause media.
matter.send_command: some_endpoint.media_playback.pause

# Upon receipt, this SHALL stop media. User experience is context-specific. This will often navigate the user back to the location where media was originally launched.
matter.send_command: some_endpoint.media_playback.stop

# Upon receipt, this SHALL Start Over with the current media playback item.
matter.send_command: some_endpoint.media_playback.start_over

# Upon receipt, this SHALL cause the handler to be invoked for "Previous". User experience is context-specific. This will often Go back to the previous media playback item.
matter.send_command: some_endpoint.media_playback.previous

# Upon receipt, this SHALL cause the handler to be invoked for "Next". User experience is context-specific. This will often Go forward to the next media playback item.
matter.send_command: some_endpoint.media_playback.next

# Upon receipt, this SHALL Rewind through media. Different Rewind speeds can be used on the TV based upon the number of sequential calls to this function. This is to avoid needing to define every speed now (multiple fast, slow motion, etc).
matter.send_command:
  path: some_endpoint.media_playback.rewind
  arguments:
    # audio_advance_unmuted:

# Upon receipt, this SHALL Advance through media. Different FF speeds can be used on the TV based upon the number of sequential calls to this function. This is to avoid needing to define every speed now (multiple fast, slow motion, etc).
matter.send_command:
  path: some_endpoint.media_playback.fast_forward
  arguments:
    # audio_advance_unmuted:

# Upon receipt, this SHALL Skip forward in the media by the given number of seconds, using the data as follows:
matter.send_command:
  path: some_endpoint.media_playback.skip_forward
  arguments:
    delta_position_milliseconds:

# Upon receipt, this SHALL Skip backward in the media by the given number of seconds, using the data as follows:
matter.send_command:
  path: some_endpoint.media_playback.skip_backward
  arguments:
    delta_position_milliseconds:

# Upon receipt, this SHALL Skip backward in the media by the given number of seconds, using the data as follows:
matter.send_command:
  path: some_endpoint.media_playback.seek
  arguments:
    position:

# Upon receipt, the server SHALL set the active Audio Track to the one identified by the TrackID in the Track catalog for the streaming media. If the TrackID does not exist in the Track catalog, OR does not correspond to the streaming media OR no media is being streamed at the time of receipt of this command, the server will return an error status of INVALID_ARGUMENT.
matter.send_command:
  path: some_endpoint.media_playback.activate_audio_track
  arguments:
    track_id:
    audio_output_index:

# Upon receipt, the server SHALL set the active Text Track to the one identified by the TrackID in the Track catalog for the streaming media. If the TrackID does not exist in the Track catalog, OR does not correspond to the streaming media OR no media is being streamed at the time of receipt of this command, the server SHALL return an error status of INVALID_ARGUMENT.
matter.send_command:
  path: some_endpoint.media_playback.activate_text_track
  arguments:
    track_id:

# If a Text Track is active (i.e. being displayed), upon receipt of this command, the server SHALL stop displaying it.
matter.send_command: some_endpoint.media_playback.deactivate_text_track
```

# MediaInput

This cluster provides an interface for controlling the Input Selector on a media device such as a TV.

```yaml
# Upon receipt, this command SHALL change the media input on the device to the input at a specific index in the Input List.
matter.send_command:
  path: some_endpoint.media_input.select_input
  arguments:
    index:

# Upon receipt, this command SHALL display the active status of the input list on screen.
matter.send_command: some_endpoint.media_input.show_input_status

# Upon receipt, this command SHALL hide the input list from the screen.
matter.send_command: some_endpoint.media_input.hide_input_status

# Upon receipt, this command SHALL rename the input at a specific index in the Input List.
matter.send_command:
  path: some_endpoint.media_input.rename_input
  arguments:
    index:
    name:
```

# LowPower

This cluster provides an interface for managing low power mode on a device.

```yaml
# This command SHALL put the device into low power mode.
matter.send_command: some_endpoint.low_power.sleep
```

# KeypadInput

This cluster provides an interface for controlling a device like a TV using action commands such as UP, DOWN, and SELECT.

```yaml
# Upon receipt, this SHALL process a keycode as input to the media endpoint.
matter.send_command:
  path: some_endpoint.keypad_input.send_key
  arguments:
    key_code: # enum: select, up, down, left, right, right_up, right_down, left_up, left_down, root_menu, setup_menu, contents_menu, favorite_menu, exit, media_top_menu, media_context_sensitive_menu, number_entry_mode, number11, number12, number0_or_number10, numbers1, numbers2, numbers3, numbers4, numbers5, numbers6, numbers7, numbers8, numbers9, dot, enter, clear, next_favorite, channel_up, channel_down, previous_channel, sound_select, input_select, display_information, help, page_up, page_down, power, volume_up, volume_down, mute, play, stop, pause, record, rewind, fast_forward, eject, forward, backward, stop_record, pause_record, reserved, angle, sub_picture, video_on_demand, electronic_program_guide, timer_programming, initial_configuration, select_broadcast_type, select_sound_presentation, play_function, pause_play_function, record_function, pause_record_function, stop_function, mute_function, restore_volume_function, tune_function, select_media_function, select_av_input_function, select_audio_input_function, power_toggle_function, power_off_function, power_on_function, f1_blue, f2_red, f3_green, f4_yellow, f5, data
```

# ContentLauncher

This cluster provides an interface for launching content on a media player device such as a TV or Speaker.

```yaml
# Upon receipt, this SHALL launch the specified content with optional search criteria.
matter.send_command:
  path: some_endpoint.content_launcher.launch_content
  arguments:
    search:
    auto_play:
    # data:
    # playback_preferences:
    # use_current_context:

# Upon receipt, this SHALL launch content from the specified URL.
matter.send_command:
  path: some_endpoint.content_launcher.launch_url
  arguments:
    content_url:
    # display_string:
    # branding_information:
```

# AudioOutput

This cluster provides an interface for controlling the Output on a media device such as a TV.

```yaml
# Upon receipt, this SHALL change the output on the device to the output at a specific index in the Output List.
matter.send_command:
  path: some_endpoint.audio_output.select_output
  arguments:
    index:

# Upon receipt, this SHALL rename the output at a specific index in the Output List.
matter.send_command:
  path: some_endpoint.audio_output.rename_output
  arguments:
    index:
    name:
```

# ApplicationLauncher

This cluster provides an interface for launching content on a media player device such as a TV or Speaker.

```yaml
# Upon receipt of this command, the server SHALL launch the application with optional data.
matter.send_command:
  path: some_endpoint.application_launcher.launch_app
  arguments:
    # application:
    # data:

# Upon receipt of this command, the server SHALL stop the application if it is running.
matter.send_command:
  path: some_endpoint.application_launcher.stop_app
  arguments:
    # application:

# Upon receipt of this command, the server SHALL hide the application.
matter.send_command:
  path: some_endpoint.application_launcher.hide_app
  arguments:
    # application:
```

# AccountLogin

This cluster provides commands that facilitate user account login on a Content App or a node. For example, a Content App running on a Video Player device, which is represented as an endpoint (see [TV Architecture]), can use this cluster to help make the user account on the Content App match the user account on the Client.

```yaml
# The purpose of this command is to determine if the active user account of the given Content App matches the active user account of a given Commissionee, and when it does, return a Setup PIN which can be used for password-authenticated session establishment (PASE) with the Commissionee.
matter.send_command:
  path: some_endpoint.account_login.get_setup_pin
  arguments:
    temp_account_identifier:

# The purpose of this command is to allow the Content App to assume the user account of a given Commissionee by leveraging the Setup PIN input by the user during the commissioning process.
matter.send_command:
  path: some_endpoint.account_login.login
  arguments:
    temp_account_identifier:
    setup_pin:
    # node:

# The purpose of this command is to instruct the Content App to clear the current user account.
matter.send_command:
  path: some_endpoint.account_login.logout
  arguments:
    # node:
```

# ContentControl

This cluster is used for managing the content control (including "parental control") settings on a media device such as a TV, or Set-top Box.

```yaml
# The purpose of this command is to update the PIN used for protecting configuration of the content control settings.
matter.send_command:
  path: some_endpoint.content_control.update_pin
  arguments:
    old_pin:
    new_pin:

# The purpose of this command is to reset the PIN.
matter.send_command: some_endpoint.content_control.reset_pin

# The purpose of this command is to turn on the Content Control feature on a media device.
matter.send_command: some_endpoint.content_control.enable

# The purpose of this command is to turn off the Content Control feature on a media device.
matter.send_command: some_endpoint.content_control.disable

# The purpose of this command is to add the extra screen time for the user.
matter.send_command:
  path: some_endpoint.content_control.add_bonus_time
  arguments:
    # pin_code:
    # bonus_time: # default: 300

# The purpose of this command is to set the ScreenDailyTime attribute.
matter.send_command:
  path: some_endpoint.content_control.set_screen_daily_time
  arguments:
    screen_time:

# The purpose of this command is to specify whether programs with no Content rating must be blocked by this media device.
matter.send_command: some_endpoint.content_control.block_unrated_content

# The purpose of this command is to specify whether programs with no Content rating must be blocked by this media device.
matter.send_command: some_endpoint.content_control.unblock_unrated_content

# The purpose of this command is to set the OnDemandRatingThreshold attribute.
matter.send_command:
  path: some_endpoint.content_control.set_on_demand_rating_threshold
  arguments:
    rating:

# The purpose of this command is to set ScheduledContentRatingThreshold attribute.
matter.send_command:
  path: some_endpoint.content_control.set_scheduled_content_rating_threshold
  arguments:
    rating:

# The purpose of this command is to set BlockChannelList attribute.
matter.send_command:
  path: some_endpoint.content_control.add_block_channels
  arguments:
    channels:

# The purpose of this command is to remove channels from the BlockChannelList attribute.
matter.send_command:
  path: some_endpoint.content_control.remove_block_channels
  arguments:
    channel_indexes:

# The purpose of this command is to set applications to the BlockApplicationList attribute.
matter.send_command:
  path: some_endpoint.content_control.add_block_applications
  arguments:
    applications:

# The purpose of this command is to remove applications from the BlockApplicationList attribute.
matter.send_command:
  path: some_endpoint.content_control.remove_block_applications
  arguments:
    applications:

# The purpose of this command is to set the BlockContentTimeWindow attribute.
matter.send_command:
  path: some_endpoint.content_control.set_block_content_time_window
  arguments:
    time_window:

# The purpose of this command is to remove the selected time windows from the BlockContentTimeWindow attribute.
matter.send_command:
  path: some_endpoint.content_control.remove_block_content_time_window
  arguments:
    time_window_indexes:
```

# ContentAppObserver

This cluster provides an interface for sending targeted commands to an Observer of a Content App on a Video Player device such as a Streaming Media Player, Smart TV or Smart Screen. The cluster server for Content App Observer is implemented by an endpoint that communicates with a Content App, such as a Casting Video Client. The cluster client for Content App Observer is implemented by a Content App endpoint. A Content App is informed of the NodeId of an Observer when a binding is set on the Content App. The Content App can then send the ContentAppMessage to the Observer (server cluster), and the Observer responds with a ContentAppMessageResponse.

```yaml
# Upon receipt, the data field MAY be parsed and interpreted.
matter.send_command:
  path: some_endpoint.content_app_observer.content_app_message
  arguments:
    # data:
    encoding_hint:
```

# ZoneManagement

This cluster provides an interface to manage regions of interest, or Zones, which can be either manufacturer or user defined.

```yaml
# This command SHALL create and store a TwoD Cartesian Zone.
matter.send_command:
  path: some_endpoint.zone_management.create_two_d_cartesian_zone
  arguments:
    zone:

# The UpdateTwoDCartesianZone SHALL update a stored TwoD Cartesian Zone.
matter.send_command:
  path: some_endpoint.zone_management.update_two_d_cartesian_zone
  arguments:
    zone_id:
    zone:

# This command SHALL remove the user-defined Zone indicated by ZoneID.
matter.send_command:
  path: some_endpoint.zone_management.remove_zone
  arguments:
    zone_id:

# This command is used to create or update a Trigger for the specified motion Zone.
matter.send_command:
  path: some_endpoint.zone_management.create_or_update_trigger
  arguments:
    trigger:

# This command SHALL remove the Trigger for the provided ZoneID.
matter.send_command:
  path: some_endpoint.zone_management.remove_trigger
  arguments:
    zone_id:
```

# CameraAVStreamManagement

The Camera AV Stream Management cluster is used to allow clients to manage, control, and configure various audio, video, and snapshot streams on a camera.

```yaml
# This command SHALL allocate an audio stream on the camera and return an allocated audio stream identifier.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.audio_stream_allocate
  arguments:
    stream_usage: # enum: internal, recording, analysis, live_view
    audio_codec: # enum: opus, aac-lc
    channel_count:
    sample_rate:
    bit_rate:
    bit_depth:

# This command SHALL deallocate an audio stream on the camera, corresponding to the given audio stream identifier.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.audio_stream_deallocate
  arguments:
    audio_stream_id:

# This command SHALL allocate a video stream on the camera and return an allocated video stream identifier.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.video_stream_allocate
  arguments:
    stream_usage: # enum: internal, recording, analysis, live_view
    video_codec: # enum: h264, hevc, vvc, av1
    min_frame_rate:
    max_frame_rate:
    min_resolution:
    max_resolution:
    min_bit_rate:
    max_bit_rate:
    key_frame_interval:
    # watermark_enabled:
    # osd_enabled:

# This command SHALL be used to modify a stream specified by the VideoStreamID.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.video_stream_modify
  arguments:
    video_stream_id:
    # watermark_enabled:
    # osd_enabled:

# This command SHALL deallocate a video stream on the camera, corresponding to the given video stream identifier.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.video_stream_deallocate
  arguments:
    video_stream_id:

# This command SHALL allocate a snapshot stream on the device and return an allocated snapshot stream identifier.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.snapshot_stream_allocate
  arguments:
    image_codec: # enum: jpeg, heic
    max_frame_rate:
    min_resolution:
    max_resolution:
    quality:
    # watermark_enabled:
    # osd_enabled:

# This command SHALL be used to modify a stream specified by the VideoStreamID.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.snapshot_stream_modify
  arguments:
    snapshot_stream_id:
    # watermark_enabled:
    # osd_enabled:

# This command SHALL deallocate an snapshot stream on the camera, corresponding to the given snapshot stream identifier.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.snapshot_stream_deallocate
  arguments:
    snapshot_stream_id:

# This command SHALL set the relative priorities of the various stream usages on the camera.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.set_stream_priorities
  arguments:
    stream_priorities: # enum: internal, recording, analysis, live_view

# This command SHALL return a Snapshot from the camera.
matter.send_command:
  path: some_endpoint.camera_av_stream_management.capture_snapshot
  arguments:
    snapshot_stream_id:
    requested_resolution:
```

# CameraAVSettingsUserLevelManagement

This cluster provides an interface into controls associated with the operation of a device that provides pan, tilt, and zoom functions, either mechanically, or against a digital image.

```yaml
# This command SHALL move the camera to the provided values for pan, tilt, and zoom in the mechanical PTZ.
matter.send_command:
  path: some_endpoint.camera_av_settings_user_level_management.mptz_set_position
  arguments:
    # pan:
    # tilt:
    # zoom:

# This command SHALL move the camera by the delta values relative to the currently defined position.
matter.send_command:
  path: some_endpoint.camera_av_settings_user_level_management.mptz_relative_move
  arguments:
    # pan_delta: # default: 0
    # tilt_delta: # default: 0
    # zoom_delta: # default: 0

# This command SHALL move the camera to the positions specified by the Preset passed.
matter.send_command:
  path: some_endpoint.camera_av_settings_user_level_management.mptz_move_to_preset
  arguments:
    preset_id:

# This command allows creating a new preset or updating the values of an existing one.
matter.send_command:
  path: some_endpoint.camera_av_settings_user_level_management.mptz_save_preset
  arguments:
    # preset_id:
    name:

# This command SHALL remove a preset entry from the PresetMptzTable.
matter.send_command:
  path: some_endpoint.camera_av_settings_user_level_management.mptz_remove_preset
  arguments:
    preset_id:

# This command allows for setting the digital viewport for a specific Video Stream.
matter.send_command:
  path: some_endpoint.camera_av_settings_user_level_management.dptz_set_viewport
  arguments:
    video_stream_id:
    viewport:

# This command SHALL change the per stream viewport by the amount specified in a relative fashion.
matter.send_command:
  path: some_endpoint.camera_av_settings_user_level_management.dptz_relative_move
  arguments:
    video_stream_id:
    # delta_x: # default: 0
    # delta_y: # default: 0
    # zoom_delta: # default: 0
```

# WebRTCTransportProvider

The WebRTC transport provider cluster provides a way for stream providers (e.g. Cameras) to stream or receive their data through WebRTC.

```yaml
# Requests that the Provider initiates a new session with the Offer / Answer flow in a way that allows for options to be passed and work with devices needing the standby flow.
matter.send_command:
  path: some_endpoint.web_rtc_transport_provider.solicit_offer
  arguments:
    stream_usage: # enum: internal, recording, analysis, live_view
    originating_endpoint_id:
    # video_stream_id:
    # audio_stream_id:
    # ice_servers:
    # ice_transport_policy:
    # metadata_enabled:
    # s_frame_config:
    # video_streams:
    # audio_streams:

# This command allows an SDP Offer to be set and start a new session.
matter.send_command:
  path: some_endpoint.web_rtc_transport_provider.provide_offer
  arguments:
    web_rtc_session_id:
    sdp:
    stream_usage: # enum: internal, recording, analysis, live_view
    originating_endpoint_id:
    # video_stream_id:
    # audio_stream_id:
    # ice_servers:
    # ice_transport_policy:
    # metadata_enabled:
    # s_frame_config:
    # video_streams:
    # audio_streams:

# This command SHALL be initiated from a Node in response to an Offer that was previously received from a remote peer.
matter.send_command:
  path: some_endpoint.web_rtc_transport_provider.provide_answer
  arguments:
    web_rtc_session_id:
    sdp:

# This command allows for string based ICE candidates generated after the initial Offer / Answer exchange, via a JSEP onicecandidate event, a DOM rtcpeerconnectioniceevent event, or other WebRTC compliant implementations, to be added to a session during the gathering phase.
matter.send_command:
  path: some_endpoint.web_rtc_transport_provider.provide_ice_candidates
  arguments:
    web_rtc_session_id:
    ice_candidates:

# This command instructs the stream provider to end the WebRTC session.
matter.send_command:
  path: some_endpoint.web_rtc_transport_provider.end_session
  arguments:
    web_rtc_session_id:
    reason: # enum: ice_failed, ice_timeout, user_hangup, user_busy, replaced, no_user_media, invite_timeout, answered_elsewhere, out_of_resources, media_timeout, low_power, privacy_mode, unknown_reason
```

# WebRTCTransportRequestor

The WebRTC transport requestor cluster provides a way for stream consumers (e.g. Matter Stream Viewer) to establish a WebRTC connection with a stream provider.

```yaml
# This command provides the stream requestor with WebRTC session details.
matter.send_command:
  path: some_endpoint.web_rtc_transport_requestor.offer
  arguments:
    web_rtc_session_id:
    sdp:
    # ice_servers:
    # ice_transport_policy:

# This command provides the stream requestor with the WebRTC session details (i.e. Session ID and SDP answer), It is the next command in the Offer/Answer flow to the ProvideOffer command.
matter.send_command:
  path: some_endpoint.web_rtc_transport_requestor.answer
  arguments:
    web_rtc_session_id:
    sdp:

# This command allows for the object based ICE candidates generated after the initial Offer / Answer exchange, via a JSEP onicecandidate event, a DOM rtcpeerconnectioniceevent event, or other WebRTC compliant implementations, to be added to a session during the gathering phase.
matter.send_command:
  path: some_endpoint.web_rtc_transport_requestor.ice_candidates
  arguments:
    web_rtc_session_id:
    ice_candidates:

# This command notifies the stream requestor that the provider has ended the WebRTC session.
matter.send_command:
  path: some_endpoint.web_rtc_transport_requestor.end
  arguments:
    web_rtc_session_id:
    reason: # enum: ice_failed, ice_timeout, user_hangup, user_busy, replaced, no_user_media, invite_timeout, answered_elsewhere, out_of_resources, media_timeout, low_power, privacy_mode, unknown_reason
```

# PushAVStreamTransport

This cluster implements the upload of Audio and Video streams from the Push AV Stream Transport Cluster using suitable push-based transports.

```yaml
# This command SHALL allocate a transport and return a PushTransportConnectionID.
matter.send_command:
  path: some_endpoint.push_av_stream_transport.allocate_push_transport
  arguments:
    transport_options:

# This command SHALL be generated to request the Node deallocates the specified transport.
matter.send_command:
  path: some_endpoint.push_av_stream_transport.deallocate_push_transport
  arguments:
    connection_id:

# This command is used to request the Node modifies the configuration of the specified push transport.
matter.send_command:
  path: some_endpoint.push_av_stream_transport.modify_push_transport
  arguments:
    connection_id:
    transport_options:

# This command SHALL be generated to request the Node modifies the Transport Status of a specified transport or all transports.
matter.send_command:
  path: some_endpoint.push_av_stream_transport.set_transport_status
  arguments:
    connection_id:
    transport_status: # enum: active, inactive

# This command SHALL be generated to request the Node to manually start the specified push transport.
matter.send_command:
  path: some_endpoint.push_av_stream_transport.manually_trigger_transport
  arguments:
    connection_id:
    activation_reason: # enum: user_initiated, automation, emergency, doorbell_pressed
    # time_control:
    # user_defined:

# This command SHALL return the Transport Configuration for the specified push transport or all allocated transports for the fabric if null.
matter.send_command:
  path: some_endpoint.push_av_stream_transport.find_transport
  arguments:
    connection_id:
```

# Chime

This cluster provides facilities to configure and play Chime sounds, such as those used in a doorbell.

```yaml
# This command will play the currently selected chime or the chime passed in.
matter.send_command:
  path: some_endpoint.chime.play_chime_sound
  arguments:
    # chime_id:
```

# AVAnalysis

This server cluster provides an interface for controlling events pertaining to AV analysis.

```yaml
# This command SHALL enable the generation of PerceivedContext events for the set of ambient context triggers that are provided, should they be detected.
matter.send_command:
  path: some_endpoint.av_analysis.enable_context_triggers
  arguments:
    context_triggers:

# This command will disable the generation of PerceivedContext events for the set of ambient context triggers that are provided, should they be detected.
matter.send_command:
  path: some_endpoint.av_analysis.disable_context_triggers
  arguments:
    context_triggers:

# This command will cause the Analysis Node to establish a video stream with a StreamUsage of Analysis Value with the identified camera.
matter.send_command:
  path: some_endpoint.av_analysis.establish_analysis_stream
  arguments:
    node_id:

# This command will cause the Analysis Node to activate either a WebRTC livestream or PushAV recording stream with a StreamUsage of Analysis Value with the identified camera.
matter.send_command:
  path: some_endpoint.av_analysis.activate_analysis_stream
  arguments:
    analysis_stream_id:
    # web_rtc_endpoint_id:
    # push_av_endpoint_id:

# This command will cause the Analysis Node to deactivate either a WebRTC livestream or PushAV recording stream with a StreamUsage of Analysis Value with the identified camera.
matter.send_command:
  path: some_endpoint.av_analysis.deactivate_analysis_stream
  arguments:
    analysis_stream_id:

# This command will cause the Analysis Node to remove a previously allocated video stream.
matter.send_command:
  path: some_endpoint.av_analysis.remove_analysis_stream
  arguments:
    analysis_stream_id:
```

# CommodityTariff

The CommodityTariffCluster provides the mechanism for communicating Commodity Tariff information within the premises.

```yaml
# The GetTariffComponent command allows a client to request information for a tariff component identifier that may no longer be available in the TariffPeriods attributes.
matter.send_command:
  path: some_endpoint.commodity_tariff.get_tariff_component
  arguments:
    tariff_component_id:

# The GetDayEntry command allows a client to request information for a calendar day entry identifier that may no longer be available in the CalendarPeriods or IndividualDays attributes.
matter.send_command:
  path: some_endpoint.commodity_tariff.get_day_entry
  arguments:
    day_entry_id:
```

# CommissionerControl

Supports the ability for clients to request the commissioning of themselves or other nodes onto a fabric which the cluster server can commission onto.

```yaml
# This command is sent by a client to request approval for a future CommissionNode call.
matter.send_command:
  path: some_endpoint.commissioner_control.request_commissioning_approval
  arguments:
    request_id:
    vendor_id:
    product_id:
    # label:

# This command is sent by a client to request that the server begins commissioning a previously approved request.
matter.send_command:
  path: some_endpoint.commissioner_control.commission_node
  arguments:
    request_id:
    # response_timeout_seconds: # default: 30
```

# JointFabricDatastore

The Joint Fabric Datastore Cluster is a cluster that provides a mechanism for the Joint Fabric Administrators to manage the set of Nodes, Groups, and Group membership among Nodes in the Joint Fabric.

```yaml
# Upon receipt, this SHALL add a KeySet to the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.add_key_set
  arguments:
    group_key_set:

# Upon receipt, this SHALL update a KeySet in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.update_key_set
  arguments:
    group_key_set:

# Upon receipt, this SHALL remove a KeySet from the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.remove_key_set
  arguments:
    group_key_set_id:

# Upon receipt, this SHALL add a group to the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.add_group
  arguments:
    group_id:
    friendly_name:
    group_key_set_id:
    group_cat:
    group_cat_version:
    group_permission: # enum: view, operate, manage, administer

# Upon receipt, this SHALL update a group in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.update_group
  arguments:
    group_id:
    friendly_name:
    group_key_set_id:
    group_cat:
    group_cat_version:
    group_permission: # enum: view, operate, manage, administer

# Upon receipt, this SHALL remove a group from the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.remove_group
  arguments:
    group_id:

# Upon receipt, this SHALL add an admin to the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.add_admin
  arguments:
    node_id:
    friendly_name:
    vendor_id:
    icac:

# Upon receipt, this SHALL update an admin entry in the AdminList attribute.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.update_admin
  arguments:
    node_id:
    # friendly_name:
    # icac:

# Upon receipt, this SHALL remove an admin from the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.remove_admin
  arguments:
    node_id:

# Upon receipt, this SHALL add a node to the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.add_pending_node
  arguments:
    node_id:
    friendly_name:

# Upon receipt, this SHALL request that Datastore information relating to a Node of the accessing fabric is refreshed.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.refresh_node
  arguments:
    node_id:

# Upon receipt, this SHALL update the friendly name for a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.update_node
  arguments:
    node_id:
    friendly_name:

# Upon receipt, this SHALL remove a node from the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.remove_node
  arguments:
    node_id:

# Upon receipt, this SHALL update the state of an endpoint for a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.update_endpoint_for_node
  arguments:
    endpoint_id:
    node_id:
    friendly_name:

# Upon receipt, this SHALL add a Group ID to an endpoint for a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.add_group_id_to_endpoint_for_node
  arguments:
    node_id:
    endpoint_id:
    group_id:

# Upon receipt, this SHALL remove a Group ID from an endpoint for a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.remove_group_id_from_endpoint_for_node
  arguments:
    node_id:
    endpoint_id:
    group_id:

# Upon receipt, this SHALL add a binding to an endpoint for a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.add_binding_to_endpoint_for_node
  arguments:
    node_id:
    endpoint_id:
    binding:

# Upon receipt, this SHALL remove a binding from an endpoint for a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.remove_binding_from_endpoint_for_node
  arguments:
    list_id:
    endpoint_id:
    node_id:

# Upon receipt, this SHALL add an ACL to a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.add_acl_to_node
  arguments:
    node_id:
    acl_entry:

# Upon receipt, this SHALL remove an ACL from a node in the Joint Fabric Datastore Cluster of the accessing fabric.
matter.send_command:
  path: some_endpoint.joint_fabric_datastore.remove_acl_from_node
  arguments:
    list_id:
    node_id:
```

# JointFabricAdministrator

An instance of the Joint Fabric Administrator Cluster only applies to Joint Fabric Administrator nodes fulfilling the role of Anchor CA.

```yaml
# This command SHALL be generated during Joint Commissioning Method and subsequently be responded in the form of an ICACCSRResponse command.
matter.send_command: some_endpoint.joint_fabric_administrator.icaccsr_request

# This command SHALL be generated and executed during Joint Commissioning Method and subsequently be responded in the form of an ICACResponse command.
matter.send_command:
  path: some_endpoint.joint_fabric_administrator.add_icac
  arguments:
    icac_value:

# This command SHALL fail with a InvalidAdministratorFabricIndex status code sent back to the initiator if the AdministratorFabricIndex attribute has the value of null.
matter.send_command:
  path: some_endpoint.joint_fabric_administrator.open_joint_commissioning_window
  arguments:
    commissioning_timeout:
    pake_passcode_verifier:
    discriminator:
    iterations:
    salt:

# This command SHALL be sent by a candidate Joint Fabric Anchor Administrator to the current Joint Fabric Anchor Administrator to request transfer of the Anchor Fabric.
matter.send_command: some_endpoint.joint_fabric_administrator.transfer_anchor_request

# This command SHALL indicate the completion of the transfer of the Anchor Fabric to another Joint Fabric Ecosystem Administrator.
matter.send_command: some_endpoint.joint_fabric_administrator.transfer_anchor_complete

# This command SHALL be used for communicating to client the endpoint that holds the Joint Fabric Administrator Cluster.
matter.send_command:
  path: some_endpoint.joint_fabric_administrator.announce_joint_fabric_administrator
  arguments:
    endpoint_id:
```

# TLSCertificateManagement

This Cluster is used to manage TLS Client Certificates and to provision
TLS endpoints with enough information to facilitate subsequent connection.

```yaml
# This command SHALL provision a newly provided certificate, or rotate an existing one, based on the contents of the CAID field.
matter.send_command:
  path: some_endpoint.tls_certificate_management.provision_root_certificate
  arguments:
    certificate:
    caid:

# This command SHALL return the specified TLS root certificate, or all provisioned TLS root certificates for the accessing fabric, based on the contents of the CAID field.
matter.send_command:
  path: some_endpoint.tls_certificate_management.find_root_certificate
  arguments:
    caid:

# This command SHALL return the CAID for the passed in fingerprint.
matter.send_command:
  path: some_endpoint.tls_certificate_management.lookup_root_certificate
  arguments:
    fingerprint:

# This command SHALL be generated to request the server removes the certificate provisioned to the provided Certificate Authority ID.
matter.send_command:
  path: some_endpoint.tls_certificate_management.remove_root_certificate
  arguments:
    caid:

# This command SHALL be generated to request the Node generates a certificate signing request for a new TLS key pair or use an existing CCDID for certificate rotation.
matter.send_command:
  path: some_endpoint.tls_certificate_management.client_csr
  arguments:
    nonce:
    ccdid:

# This command SHALL be generated to request the Node provisions newly provided Client Certificate Details, or rotate an existing client certificate.
matter.send_command:
  path: some_endpoint.tls_certificate_management.provision_client_certificate
  arguments:
    ccdid:
    client_certificate:
    intermediate_certificates:

# This command SHALL return the TLSClientCertificateDetailStruct for the passed in CCDID, or all TLS client certificates for the accessing fabric, based on the contents of the CCDID field.
matter.send_command:
  path: some_endpoint.tls_certificate_management.find_client_certificate
  arguments:
    ccdid:

# This command SHALL return the CCDID for the passed in Fingerprint.
matter.send_command:
  path: some_endpoint.tls_certificate_management.lookup_client_certificate
  arguments:
    fingerprint:

# This command SHALL be used to request the Node removes all stored information for the provided CCDID.
matter.send_command:
  path: some_endpoint.tls_certificate_management.remove_client_certificate
  arguments:
    ccdid:
```

# TLSClientManagement

This Cluster is used to provision TLS Endpoints with enough information to facilitate subsequent connection.

```yaml
# This command is used to provision a TLS Endpoint for the provided Hostname / Port combination.
matter.send_command:
  path: some_endpoint.tls_client_management.provision_endpoint
  arguments:
    hostname:
    port:
    caid:
    ccdid:
    endpoint_id:

# This command is used to find a TLS Endpoint by its ID.
matter.send_command:
  path: some_endpoint.tls_client_management.find_endpoint
  arguments:
    endpoint_id:

# This command is used to remove a TLS Endpoint by its ID.
matter.send_command:
  path: some_endpoint.tls_client_management.remove_endpoint
  arguments:
    endpoint_id:
```

# UnitTesting

The Test Cluster is meant to validate the generated code

```yaml
# Simple command without any parameters and without a specific response.
# To aid in unit testing, this command will re-initialize attribute storage to defaults.
matter.send_command: some_endpoint.unit_testing.test

# Simple command without any parameters and without a specific response not handled by the server
matter.send_command: some_endpoint.unit_testing.test_not_handled

# Simple command without any parameters and with a specific response
matter.send_command: some_endpoint.unit_testing.test_specific

# Simple command that should not be added to the server.
matter.send_command: some_endpoint.unit_testing.test_unknown_command

# Command that takes two arguments and returns their sum.
matter.send_command:
  path: some_endpoint.unit_testing.test_add_arguments
  arguments:
    arg1:
    arg2:

# Command that takes an argument which is bool
matter.send_command:
  path: some_endpoint.unit_testing.test_simple_argument_request
  arguments:
    arg1:

# Command that takes various arguments that are arrays, including an array of structs which have a list member.
matter.send_command:
  path: some_endpoint.unit_testing.test_struct_array_argument_request
  arguments:
    arg1:
    arg2:
    arg3: # enum: unspecified, value_a, value_b, value_c
    arg4:
    arg5: # enum: unspecified, value_a, value_b, value_c
    arg6:

# Command that takes an argument which is struct. The response echoes the
# 'b' field of the single arg.
matter.send_command:
  path: some_endpoint.unit_testing.test_struct_argument_request
  arguments:
    arg1:

# Command that takes an argument which is nested struct. The response
# echoes the 'b' field of ar1.c.
matter.send_command:
  path: some_endpoint.unit_testing.test_nested_struct_argument_request
  arguments:
    arg1:

# Command that takes an argument which is a list of structs. The response
# returns false if there is some struct in the list whose 'b' field is
# false, and true otherwise (including if the list is empty).
matter.send_command:
  path: some_endpoint.unit_testing.test_list_struct_argument_request
  arguments:
    arg1:

# Command that takes an argument which is a list of INT8U. The response
# returns false if the list contains a 0 in it, true otherwise (including
# if the list is empty).
matter.send_command:
  path: some_endpoint.unit_testing.test_list_int8_u_argument_request
  arguments:
    arg1:

# Command that takes an argument which is a Nested Struct List. The
# response returns false if there is some struct in arg1 (either directly
# in arg1.c or in the arg1.d list) whose 'b' field is false, and true
# otherwise.
matter.send_command:
  path: some_endpoint.unit_testing.test_nested_struct_list_argument_request
  arguments:
    arg1:

# Command that takes an argument which is a list of Nested Struct List.
# The response returns false if there is some struct in arg1 (either
# directly in as the 'c' field of an entry 'd' list of an entry) whose 'b'
# field is false, and true otherwise (including if the list is empty).
matter.send_command:
  path: some_endpoint.unit_testing.test_list_nested_struct_list_argument_request
  arguments:
    arg1:

# Command that takes an argument which is a list of INT8U and expects a
# response that reverses the list.
matter.send_command:
  path: some_endpoint.unit_testing.test_list_int8_u_reverse_request
  arguments:
    arg1:

# Command that sends a vendor id and an enum. The server is expected to
# echo them back.
matter.send_command:
  path: some_endpoint.unit_testing.test_enums_request
  arguments:
    arg1:
    arg2: # enum: unspecified, value_a, value_b, value_c

# Command that takes an argument which is nullable and optional. The
# response returns a boolean indicating whether the argument was present,
# if that's true a boolean indicating whether the argument was null, and
# if that' false the argument it received.
matter.send_command:
  path: some_endpoint.unit_testing.test_nullable_optional_request
  arguments:
    # arg1:

# Command that takes various arguments which can be nullable and/or optional. The
# response returns information about which things were received and what
# their state was.
matter.send_command:
  path: some_endpoint.unit_testing.test_complex_nullable_optional_request
  arguments:
    nullable_int:
    # optional_int:
    # nullable_optional_int:
    nullable_string:
    # optional_string:
    # nullable_optional_string:
    nullable_struct:
    # optional_struct:
    # nullable_optional_struct:
    nullable_list: # enum: unspecified, value_a, value_b, value_c
    # optional_list: # enum: unspecified, value_a, value_b, value_c
    # nullable_optional_list: # enum: unspecified, value_a, value_b, value_c

# Command that takes an argument which is a struct. The response echoes
# the struct back.
matter.send_command:
  path: some_endpoint.unit_testing.simple_struct_echo_request
  arguments:
    arg1:

# Command that just responds with a success status if the timed invoke
# conditions are met.
matter.send_command: some_endpoint.unit_testing.timed_invoke_request

# Command that takes an optional argument which is bool. It responds with a success value if the optional is set to any value.
matter.send_command:
  path: some_endpoint.unit_testing.test_simple_optional_argument_request
  arguments:
    # arg1:

# Command that takes identical arguments to the fields of the TestEvent and logs the TestEvent to the buffer. Command returns an event ID as the response.
matter.send_command:
  path: some_endpoint.unit_testing.test_emit_test_event_request
  arguments:
    arg1:
    arg2: # enum: unspecified, value_a, value_b, value_c
    arg3:

# Command that takes identical arguments to the fields of the TestFabricScopedEvent and logs the TestFabricScopedEvent to the buffer. Command returns an event ID as the response.
matter.send_command:
  path: some_endpoint.unit_testing.test_emit_test_fabric_scoped_event_request
  arguments:
    arg1:

# Command that responds after sleepBeforeResponseTimeMs with an octet_string the size requested with fillCharacter.
matter.send_command:
  path: some_endpoint.unit_testing.test_batch_helper_request
  arguments:
    sleep_before_response_time_ms:
    size_of_response_buffer:
    fill_character:

# Second command that responds after sleepBeforeResponseTimeMs with an octet_string the size requested with fillCharacter.
matter.send_command:
  path: some_endpoint.unit_testing.test_second_batch_helper_request
  arguments:
    sleep_before_response_time_ms:
    size_of_response_buffer:
    fill_character:

# Command that takes an argument which is an octet string. The response echoes
# the string back. If the string is large then it would require a session that
# supports large payloads.
matter.send_command:
  path: some_endpoint.unit_testing.string_echo_request
  arguments:
    payload:

# Command that takes arguments that are global structs/enums and the
# response just echoes them back.
matter.send_command:
  path: some_endpoint.unit_testing.global_echo_request
  arguments:
    field1:
    field2: # enum: some_value, some_other_value, final_value

# Command that returns Success if the CommandFlags pass all checks at the IM layer.
# Otherwise, return appropriate StatusCode back.
matter.send_command: some_endpoint.unit_testing.test_check_command_flags

# Command having a different MEI vendor ID than the cluster. Also emits TestDifferentVendorMeiEvent.
matter.send_command:
  path: some_endpoint.unit_testing.test_different_vendor_mei_request
  arguments:
    arg1:
```

# FaultInjection

The Fault Injection Cluster provide a means for a test harness to configure faults(for example triggering a fault in the system).

```yaml
# Configure a fault to be triggered deterministically
matter.send_command:
  path: some_endpoint.fault_injection.fail_at_fault
  arguments:
    type: # enum: unspecified, system_fault, inet_fault, chip_fault, cert_fault
    id:
    num_calls_to_skip:
    num_calls_to_fail:
    take_mutex:

# Configure a fault to be triggered randomly, with a given probability defined as a percentage
matter.send_command:
  path: some_endpoint.fault_injection.fail_randomly_at_fault
  arguments:
    type: # enum: unspecified, system_fault, inet_fault, chip_fault, cert_fault
    id:
    percentage:
```

# SampleMEI

The Sample MEI cluster showcases a cluster manufacturer extensions

```yaml
# Simple command without any parameters and without a response.
matter.send_command: some_endpoint.sample_mei.ping

# Command that takes two uint8 arguments and returns their sum.
matter.send_command:
  path: some_endpoint.sample_mei.add_arguments
  arguments:
    arg1:
    arg2:
```
